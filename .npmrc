# Isolated node_modules (the pnpm default): each workspace package keeps its own
# node_modules of symlinks, which is what the Docker runner stage copies.
strict-peer-dependencies=false
auto-install-peers=true
shamefully-hoist=false
